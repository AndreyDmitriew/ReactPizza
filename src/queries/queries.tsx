import { getAllPizzas } from '../core/api';

// export const getAllPizza = async (set) => {
//   try {
//     set({ isLoading: true });
//     const response = await getAllPizzas();
//     set({ isLoading: false, data: response });
//   } catch (error) {
//     set({ error: error, isLoading: false });
//     throw new Error(`Error! status: ${error}`);
//   }
// };
